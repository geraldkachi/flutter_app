import 'package:flutter/material.dart';

class PersonTicket extends StatelessWidget {
  const PersonTicket({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("PersonTicket");
  }
}