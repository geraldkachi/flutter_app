import 'package:flutter/material.dart';

class HomeBrew extends StatelessWidget {
  const HomeBrew({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Homebrew'),
    );
  }
}